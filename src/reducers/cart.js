// import {
//   ADD_PIZZA
// } from './actions'

function cart(state = 'pizza!', action) {
  switch (action.type) {
    // case ADD_PIZZA:
    //   return stata with a pizza or something
    default:
      return state
  }
}

export default cart
